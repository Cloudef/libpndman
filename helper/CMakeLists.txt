FIND_LIBRARY(MATH_LIBRARY m)
MARK_AS_ADVANCED(MATH_LIBRARY)
SET(MILKY_SRC milky.c)
INCLUDE_DIRECTORIES(${libpndman_SOURCE_DIR}/include)
ADD_EXECUTABLE(milky ${MILKY_SRC})
TARGET_LINK_LIBRARIES(milky pndman ${LIBPNDMAN_LIBRARIES} ${MATH_LIBRARY})
INSTALL(TARGETS milky DESTINATION bin)
